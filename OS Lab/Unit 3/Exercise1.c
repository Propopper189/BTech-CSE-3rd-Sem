#include <stdio.h>
#include <fcntl.h>
#include <sys/stat.h>
#include <unistd.h>

int main()
{
    int fd1, fd2, choice;
    char src[10], dst[10];
    scanf("%s", src);
    scanf("%s", dst);
    scanf("%d", &choice);
    if(choice == 2)
    {
        
    }
    return 0;
}