#include <stdio.h>
    
int main (){
    char st[] = "Aquib";
    // for(int i = 0; i<5; i++)
    // {
    //     printf("%c", st[i]);
    //     // we can use this to print names.
    // }
    
    // We can directly use "%s" in printf instead of loops
    // to print strings.

    printf("%s", st);

    return 0;
}