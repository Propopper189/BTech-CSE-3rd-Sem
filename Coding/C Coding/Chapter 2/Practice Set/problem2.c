#include <stdio.h>
int main (){

    // What data type will 3.0/8 – 2 return?
    float a = 3.0/8-2; //data type will be float if there is float in the operands.
    printf("%f", a);
    return 0;

    //The data type will be float.
}