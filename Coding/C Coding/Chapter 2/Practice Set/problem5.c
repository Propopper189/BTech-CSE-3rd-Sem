#include <stdio.h>
int main()
{
    // 3.0 + 1 will be:
    // a. Integer.
    // b. Floating point number.
    // c. Character

    // Ans : - As we know, operation between an int and float will be float.
    float a = 3.0 + 1; // we can do the calculation in single line like this.
    printf("The value is equal to %f", a);
    return 0;
}